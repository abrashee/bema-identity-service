package com.identity.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class AppConfig {
    // reserved for infra-driven shared beans (no hardcoded config allowed)
}